---
name: app-interaction-motion
description: 为 App 和 Web 界面设计、实现或评审有质感的交互动效，涵盖磁吸交互、液态形变、3D 视差、滚动聚焦、流体 Tab、图片展开、手势驱动转场和物理碰撞。适用于跟手反馈、共享元素转场、动效质感优化；普通静态界面设计无需单独调用。
---

# App 交互动效

将距离、速度、空间层级和用户输入映射为可理解的视觉反馈。动效应有明确节奏和符合直觉的运动规律，让用户感到自己在控制界面。

## 工作方式

1. 从用户任务和现有界面确定要表达的关系：靠近目标、拖拽速度、前后层级、当前焦点、选择变化、同一内容展开、转场进度或元素碰撞。选用必要的模式，不默认集齐八种。
2. 沿用项目已有平台、组件和动画系统。先确认交互对象、输入方式、起止状态、可取消路径及可读性要求；能从代码或上下文判断的内容直接判断。
3. 阅读 [八种动效的实现参考](references/motion-patterns.md) 中相关章节。为选中的模式明确“输入 → 映射 → 视觉属性 → 松手/完成行为”，再设计或实现。
4. 拖动阶段由实际输入驱动；释放后才使用惯性、弹簧或补间收尾。允许从当前呈现状态打断并重新接管，避免快速连续操作时跳回初始值。
5. 按任务交付设计说明、参数方案、代码或评审意见。对于实现任务，在目标运行环境验证实际交互；无法运行时明确尚未验证的部分，不把数学描述当成运行结果。

## 模式选择

| 用户需要感知的关系 | 动效模式 | 核心规则 |
| --- | --- | --- |
| 元素靠近并对齐目标 | 磁吸交互 | 先有轻微阻力，再贴合；越近吸力越强 |
| 拖动对象的速度和惯性 | 液态形变 | 形变跟随运动状态，静止时恢复 |
| 卡片内的前后深度 | 3D 视差 | 前层移动多，后层移动少 |
| 滚动内容中的当前项 | 滚动聚焦 | 越靠近中心或选定焦点，越突出 |
| 导航的选择状态变化 | 流体 Tab | 前沿先走，后沿追赶，最后恢复宽度 |
| 缩略图与大图是同一对象 | 图片展开 | 保持内容连续，改变容器和裁切范围 |
| 用户直接控制页面切换 | 手势驱动转场 | 指尖位移决定进度，可前进也可回退 |
| 多个元素相互影响 | 物理碰撞 | 共用质量、阻尼和碰撞规则 |

## 共通实现约束

- 保留状态连续性：位置、速度、缩放和裁切应从当前值继续。反向拖动、重新点击、取消时不瞬移、不闪烁。
- 参数有界：限制最大形变、视差、速度和吸力；不要直接使用距离的倒数造成零距离发散。示例参数不是跨设备标准。
- 时间独立：逐帧插值、阻尼和速度计算考虑真实时间间隔；不同刷新率下保持相近手感。长时间离开页面后不要用一个巨大时间步推进物理状态。
- 视觉变化不破坏操作：保持命中区域、焦点顺序和主要文字可读；滚动聚焦不得让非焦点内容无法访问。
- 尊重减少动态效果偏好。保留状态和空间关系，降低或取消非必要形变、视差、弹跳；为拖拽提供适当的点击或键盘操作途径。
- 性能按实际设备观察。优先复用已有动画能力，避免每帧交替读取布局和写入样式；复杂物理仅在场景需要时引入。

以上工程约束是对视频原则的实现补充，不是视频逐字结论。视频来源、章节时间和提取范围见 [来源摘要](references/video-source.md)，仅在追溯出处时读取。

## 验收

针对选中的模式检查：

- 慢速拖动、快速拖动、反向移动是否产生相符反馈？松手、取消、重复操作能否稳定收尾？
- 动效是否解释了对齐、焦点、层级或状态变化，而非仅增加无关运动？
- 图片展开与返回是否保持同一内容及正确源位置？手势未完成是否能回到有效状态？
- 设备尺寸变化、低帧率和减少动态效果模式下，内容与功能是否仍可用？

仅报告实际完成的验证。设计类交付无需声称已经运行或通过设备测试。
